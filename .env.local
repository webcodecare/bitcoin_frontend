# Local development overrides - only used for local development
# Leave API_BASE_URL empty to use Vite proxy (recommended for local development)
VITE_API_BASE_URL=
VITE_WS_URL=ws://localhost:5000

# Environment
NODE_ENV=development